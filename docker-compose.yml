version: '3'
services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=${NODE_ENV}
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "100M"
        max-file: "3"
